# Shared settings for local environment
kafka:
  bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
  topics:
    api: api
    js: js
    domain: domain
    subdomain: subdomain
  groups:
    certsh: certsh
    getjs: getjs
    linkfinder: linkfinder
    db: db
